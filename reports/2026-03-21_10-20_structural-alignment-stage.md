# REPORT: structural_alignment_stage

## 1. Summary
Выполнен отдельный этап **structural alignment** без внедрения нового ML/feature/decision/toto кода.
Зафиксирована текущая структура проекта, проведено сравнение с целевой архитектурой ТЗ и добавлены недостающие директории/пакеты для безопасного выравнивания.
Подготовлена таблица соответствия путей и план переноса без поломки импортов.

## 2. Goal
Цель этапа — сначала выровнять файловую структуру относительно ТЗ, а уже потом переходить к доработкам features/model/decision/toto.

## 3. Scope
В scope вошло:
- фиксация реальной структуры репозитория на текущем состоянии;
- сопоставление целевых путей ТЗ и фактических путей;
- создание отсутствующих директорий и базовых файлов-заглушек/совместимости;
- описание безопасного плана миграции импортов.

В scope не входило:
- разработка новой логики моделей;
- изменение алгоритмов feature engineering;
- изменение decision/toto бизнес-логики.

## 4. Files Changed
- [NEW] core/__init__.py
- [NEW] core/features/__init__.py
- [NEW] core/model/__init__.py
- [NEW] core/decision/__init__.py
- [NEW] toto/__init__.py
- [NEW] database/__init__.py
- [NEW] features/__init__.py
- [NEW] model/__init__.py
- [NEW] decision/__init__.py
- [NEW] reports/2026-03-21_10-20_structural-alignment-stage.md

## 5. Detailed Changes
Текущая реальная структура (ключевые каталоги):
- `football_ai/core/features`, `football_ai/core/model`, `football_ai/core/decision`
- `football_ai/toto`
- `football_ai/database`

Сопоставление с ТЗ:

| Путь по ТЗ | Фактический путь | Статус |
|---|---|---|
| `core/` | `football_ai/core/` | не совпадает |
| `core/features/` | `football_ai/core/features/` | не совпадает |
| `core/model/` | `football_ai/core/model/` | не совпадает |
| `core/decision/` | `football_ai/core/decision/` | не совпадает |
| `toto/` | `football_ai/toto/` | не совпадает |
| `database/` | `football_ai/database/` | не совпадает |
| `features/` | отсутствует | отсутствует |
| `model/` | отсутствует | отсутствует |
| `decision/` | отсутствует | отсутствует |

Что сделано в выравнивании:
- добавлены compatibility-пакеты `core/`, `core/features/`, `core/model/`, `core/decision/`, `toto/`, `database/` с реэкспортом из `football_ai.*`;
- добавлены отсутствующие директории `features/`, `model/`, `decision/` с `__init__.py`-заглушками для фиксации целевой архитектуры.

## 6. Public Contracts / Interfaces
Публичные интерфейсы приложения не менялись.
Новые top-level пакеты добавлены как совместимый слой и не ломают текущие импорты `football_ai.*`.

## 7. Database Changes
Изменений схемы БД и миграций нет.

## 8. API Behavior
API поведение не изменено.

## 9. Business Logic Changes
Новая бизнес-логика не добавлялась.
Изменения ограничены структурой проекта и compatibility-слоем импортов.

## 10. Tests
Выполнены инфраструктурные проверки:
- `python scripts/validate_reports.py --mode staged`
- `python -m py_compile` для новых `__init__.py` файлов

Unit/integration тесты доменной логики не запускались, так как алгоритмы не менялись.

## 11. Manual Verification
Проверено вручную:
- новые директории и файлы созданы;
- импорты совместимости указывают на действующие модули `football_ai.*`;
- новый отчет соответствует обязательной структуре из 17 разделов.

## 12. Risks / Known Issues
Риск: временно присутствует дублирующая структура (и `football_ai/*`, и top-level compatibility-пакеты), что может путать при дальнейшей разработке.
Риск: при частичной миграции импортов возможно смешение двух стилей импортов до полного завершения структурного перехода.

## 13. Deviations from Spec
Отклонений от нового плана нет: этап выполнен как отдельный structural alignment до внедрения новой функциональности.

## 14. Next Recommended Step
Безопасный план переноса без поломки импортов:
1. Зафиксировать единый стандарт импортов для новых модулей (через top-level target-path).
2. Мигрировать импорты поэтапно модуль за модулем с тестами после каждого шага.
3. После полной миграции удалить compatibility-реэкспорты, оставив только целевую структуру.
4. Только после завершения выравнивания переходить к новым задачам по features/model/decision/toto.

## 15. Commit / PR
Изменения оформлены отдельным delivery-этапом structural alignment с обязательным markdown-отчетом.
PR должен содержать только структурные изменения и отчетность этого этапа.

## 16. Critical Review
Допущение: целевая архитектура ТЗ подразумевает top-level пути (`core/`, `toto/`, `database/`, `features/`, `model/`, `decision/`).
Edge-case обработан: чтобы не ломать рабочие импорты, применен слой совместимости вместо резкого переноса файлов.
Дальнейшее усиление: добавить автоматическую проверку на смешение старого и нового стилей импортов в линтер/CI.

## 17. Что не сделано из запланированного
- Не выполнена полная миграция всех импортов на целевые top-level пути.
- Причина: в этом этапе приоритет — безопасная фиксация структуры и создание каркаса без риска регрессий.
- План закрытия: выполнить пошаговый migration-phase отдельными PR после подтверждения alignment-этапа.
